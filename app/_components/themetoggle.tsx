"use client";

// FunChat uses a fixed ocean-dark theme — this component is a placeholder
// for future light/dark mode toggle if you add one.
// Currently renders nothing since the theme is hardcoded via CSS variables in globals.css.

export default function ThemeToggle() {
  return null;
}